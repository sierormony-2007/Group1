#include<iostream>
#include<string>
using namespace std;
#include "node.hpp"
class Doublylinkedlist{
    private:
       DNode *head, *tail;
       int n;
    public:
    Doublylinkedlist(){
        head = nullptr;
        tail = nullptr;
        n=0;
    }
    void deleteAt(){
      


       
    }
};